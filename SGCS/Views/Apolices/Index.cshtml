@model IEnumerable<Business.Models.Apolice>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Cliente
        </th>
        <th>
            Filial
        </th>
        <th>
            Produto
        </th>
        <th>
            Proposta
        </th>
        <th>
            Seguradora
        </th>
        <th>
            Indicação
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SituacaoDaApolice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Filial.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produto.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proposta.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Seguradora.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Indicacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SituacaoDaApolice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
